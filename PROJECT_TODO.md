# Meeting Note Taker — خطة العمل والتقدّم

مشروع: منصّة تفريغ وتحليل الاجتماعات (ثنائية اللغة: عربي/إنجليزي).

آخر تحديث: يجري العمل عليه.

---

## المطلوب من المستخدم (المتطلبات)

1. **قاعدة بيانات Supabase متكاملة** — جداول حقيقية بدل الـ mock data. ✅
2. **تحسين الواجهة (UI)** لتكون مرتّبة واحترافية أكثر (ألوان + تنظيم). ⏳
3. **إخراج المشروع لايف (Live)** + إضافة الـ API اللازمة. ⏳
4. **ضبط نظام الحسابات (Auth)** — تسجيل دخول/اشتراك بالبريد وكلمة المرور عبر Supabase. ⏳
5. **تحليل الذكاء الاصطناعي مفصّل ومرتّب** (ملخص + مهام + نقاط رئيسية). ⏳
6. **Timeline زمني** — يوضّح في الوقت الفلاني تم مناقشة كذا. ⏳
7. **منصّة مرتّبة أكثر** من ناحية الألوان والتنظيم العام. ⏳

### القرارات المتفق عليها
- **Auth:** Email + Password عبر Supabase Auth.
- **الذكاء الاصطناعي:** مفتاح OpenAI (تحليل + تفريغ صوت Whisper).
- **الصوت:** تفريغ صوت حقيقي (Whisper).
- **اللغة:** دعم العربية والإنجليزية معًا مع إمكانية التبديل (RTL/LTR).

---

## ما تم إنجازه ✅

- [x] فحص المشروع الحالي وفهم البنية (Next.js 16 + mock data).
- [x] ربط Supabase والتحقق من متغيّرات البيئة.
- [x] إنشاء مخطّط قاعدة البيانات: `meetings`, `transcript_segments`, `timeline_items`, `action_items`, `speakers` + RLS.
- [x] إنشاء bucket تخزين للملفات الصوتية (`recordings`) مع سياسات RLS.
- [x] تثبيت الحزم: `@supabase/supabase-js`, `@supabase/ssr`, `openai`, `ai`, `@ai-sdk/openai`.
- [x] إعداد ملفات Supabase (client/server/proxy) + middleware + auth callback.
- [x] طلب مفتاح `OPENAI_API_KEY` من المستخدم.
- [x] طبقة الأنواع (`lib/types.ts`) وطبقة الاستعلامات (`lib/data/meetings.ts`).

## قيد العمل ⏳

- [ ] وحدة معالجة الذكاء الاصطناعي (تفريغ Whisper + تحليل GPT: ملخص/تايم لاين/مهام).
- [ ] مسارات API: رفع/تسجيل → تفريغ → تحليل → حفظ في قاعدة البيانات.
- [ ] ربط صفحات (Record / Upload / Library / Analysis) بالبيانات الحقيقية.
- [ ] صفحات الحسابات (تسجيل دخول / اشتراك / تأكيد) وحماية المسارات.
- [ ] إعادة تصميم وتحسين الواجهة عبر كل الصفحات.

## متبقّي / لاحقًا 📋

- [ ] التحقق النهائي من التدفّق الكامل في المتصفّح.
- [ ] رفع كل شيء على GitHub بكوميت أخير.
- [ ] إخراج المشروع لايف (Deploy).

---

## ملاحظات تقنية

- **Framework:** Next.js 16 (App Router).
- **DB:** Supabase Postgres مع Row Level Security على كل الجداول (scoping بالـ `user_id`).
- **التخزين:** Supabase Storage bucket `recordings` (خاص لكل مستخدم).
- **الذكاء:** OpenAI Whisper للتفريغ + نموذج GPT للتحليل المنظّم (JSON).
